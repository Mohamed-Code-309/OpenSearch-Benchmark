PORT=3000

# Database (required)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=password
DB_NAME=opensearch_benchmark

# OpenSearch (required)
OPENSEARCH_NODE=http://localhost:9200
# Leave blank if OpenSearch has no auth (local dev without security plugin)
OPENSEARCH_USERNAME=
OPENSEARCH_PASSWORD=
